# Роадмап

Живой документ: обновляется вместе с работой, а не задним числом. Правило
одно — **сделанным считается то, что проверено**, и рядом с пунктом стоит,
чем именно. «Должно работать» и «я не менял это место» доказательствами не
являются.

Обновлено: 23.09.2026

---

## Где мы сейчас

Фаза 0 закрыта. Фаза 1 закрыта по главному пункту: **калькулятор перенесён
и принят** — 129 блюд из 130 совпадают с эталоном старого бота полностью,
последнее расхождение названо и объяснено.

Данные всех четырёх таблиц лежат в базе, читаются пакетно, схема
развёрнута.

**Фаза 2 закрыта.** PR №11 и №12 слиты в `main` 23.09.2026 (0d369fd5),
сайт работает на `https://art.karppacho.ru`: вход, справочник, блюда,
карточка блюда, сверка. Сервер с 23.09.2026 стоит на `main`.

**Данные на сайте — снимок импорта от 10.09.2026.** Синхронизации «лист →
БД» ещё нет: правки шефа в таблице на сайт сами не попадают.

**Решения Александра от 23.09.2026:**
- платформа будет **писать в Google-таблицу**: таблица остаётся у шефа,
  править можно и там, и на сайте; одновременная правка одной ячейки —
  конфликт, а не затирание;
- боты не вернутся — флаг `BOTS_ALIVE` снимается;
- штучные позиции с весом 1–2 г в ТТК — платформа предупреждает, шеф
  правит лист.

**Порядок работ дальше:**
1. Предупреждение о штучных позициях с нетто меньше 10 % веса штуки — у
   26 блюд себестоимость занижена.
2. Синхронизация «лист → БД» раз в 5 минут.
3. Фильтры и сортировка по всем колонкам справочника и блюд, как в
   Google-таблицах.
4. Запись из платформы в таблицу: очередь, сверка ячейки перед записью,
   журнал правок; снятие `BOTS_ALIVE`.
5. Карточки ингредиентов по этикетке — замена `pizza_bot_new`.
6. Редактирование справочника и блюд прямо в таблицах экранов.

Потом — расчётка и чат с ассистентом.

**Три Telegram-бота остановлены** с 08.09.2026: Telegram закрыл последний
доступный IP, и с российского VPS его API недоступен. Это не решение, а
обстоятельство — шеф сейчас без инструментов, и это делает фазы 2–3
срочными.

---

## Фаза 0 — каркас, безопасность, импорт ✅

| | Чем доказано |
|---|---|
| Ротация секретов | **Пропущена** по решению Александра, осознанный риск |
| Разведка среды | Память: 581 МБ на стек Supabase, свободно 6.4 из 7.8 Гб |
| Каркас качества | CI зелёный, 6 задач, самая долгая 34 с при бюджете 300 |
| Периметр | `ss` снаружи: слушает только `sshd`; UFW 22/80/443 |
| Данные | Отчёт прочитал все 10 листов, расхождений заголовков нет |

Дополнительно сделано сверх плана: боты сняты с сервера и заархивированы
(177 Мб, sha256 сверен, копия вне сервера), сервер перезагружен на ядро
6.8.0-139, установлен Docker 29.8.0.

**Что выяснилось и стоило времени:**

- Шлюз Supabase — Envoy (`api-gw`), а не Kong; upstream перестроился.
- `COMPOSE_FILE` в `.env` молча отключает оверлей, и `docker compose config`
  при этом не ругается.
- Порты нельзя ограничить переменными: `POSTGRES_PORT` уходит внутрь
  контейнера как `PGPORT`. Привязка только оверлеем и только с `!override` —
  списки портов при слиянии дописываются, а не заменяются.

---

## Фаза 1 — склейка справочника и эквивалентность расчёта ✅

Главная работа проекта: справочник ингредиентов расколот надвое, и
калькулятор не видит того, что оформили повара.

**Замер на живых данных (09.09.2026):** 107 карточек, 91 склеилась
однозначно, **16 требуют решения человека** — 5 тёзок, 4 кандидата,
7 без пары вовсе.

- [x] Схема БД: 8 таблиц, первая миграция, обратимость проверена на боевом Postgres
- [x] Сопоставление имён: точное совпадение решает только если оно одно
- [x] Пакетное чтение: два обращения на таблицу вместо одного на лист.
      Проверено двумя прогонами отчёта подряд без 429
- [x] Импорт листов в БД + запись прогона в `sync_runs`.
      Перенесено: 130 ингредиентов, 28 упаковок, 15 способов, 130 блюд,
      701 строка ТТК, 101 карточка
- [x] Перенос калькулятора себестоимости из `kitchen_bot`
- [x] **Приёмка: 129 из 130 блюд совпадают с эталоном**, одно расхождение
      названо в `KNOWN_DIVERGENCES`. Прогон: `scripts/verify_golden.py`,
      код возврата 0
- [x] Экран ручной сверки — сделан в фазе 2 (экран «Сверка справочника»)
- [ ] Поллинг раз в 5 минут (пакетное чтение уже есть, нужен воркер).
      **Второй в порядке работ:** данные на сайте — снимок 10.09.2026 00:34

**Найдено по дороге.** В листе `ING` строки 132–169 не имеют id — 38 строк.
Отсюда расхождение «168 строк в листе, 130 ингредиентов». Импорт их
пропускает и называет поимённо; что это за блок, стоит посмотреть шефу.

**Открытый вопрос.** В таблице карточек семь листов, а не один:
`Фритюрные продукты`, `Топпинги`, `Овощи`, `Соусы`, `Заявки`. Бот пишет
только в `Лист1`, остальные ведут руками — там могут быть ещё ингредиенты,
и тогда «сирот» окажется больше семи.

---

## Фаза 2 — веб только на чтение ✅

Бэкенд был готов, интерфейса не было вовсе. Фронтенд построен: устройство
описано в [спеке](superpowers/specs/2026-09-10-frontend-faza-2-design.md),
работа шла по [плану](superpowers/plans/2026-09-10-frontend-faza-2.md) из
шестнадцати задач, каждую смотрел отдельный ревьюер. Решения, принятые по
ходу (Ruling 1–75), — в [журнале решений](superpowers/plans/2026-09-10-frontend-faza-2-resheniya.md).

**Выложено 23.09.2026 и слито в `main`** — PR №11 и №12, 0d369fd5. Простой
сервера 18–22.09 оказался выключением ВМ со стороны гипервизора AdminVPS,
а не поломкой (разбор — в журнале CLAUDE.md).

**Сделано**

- [x] Вход: Supabase Auth, проверка подписи JWT в FastAPI, роли в `profiles`.
      Проверено насквозь: пароль → токен → `/api/me` с ролями `chef` и
      `developer`; без токена все ручки отдают 401
- [x] Миграция профилей и ролей применена на боевой базе, справочник ролей
      наполнен четырьмя значениями
- [x] Ручки чтения: `/api/ingredients`, `/api/dishes`, `/api/dishes/{id}`,
      `/api/reconciliation`. `/api/dishes` отдаёт 130 блюд, B001 показывает
      UC 84.66 — сходится с эталоном
- [x] Заведение доступа: `scripts/grant_access.py`, идемпотентен, пароль
      печатает один раз
- [x] **Вход своими ручками.** `POST /api/auth/login`, `/refresh`, `/logout`:
      в GoTrue ходит сервер, браузер получает httpOnly-куки и токена не
      видит. Раздел 3 ТЗ предлагал ходить в Supabase прямо из браузера — так
      нельзя: GoTrue слушает 127.0.0.1, наружу не проксируется, и наш CSP
      такой запрос запретил бы. Отказ по существу (400 от GoTrue) даёт 401,
      недоступность и неверный anon-ключ — 502
- [x] **Каркас фронтенда:** React + TypeScript + Vite, своё CSS, шрифт
      IBM Plex в сборке — приложение не зависит от Google, чтобы отрисовать
      цифры
- [x] **Компоненты показа:** `<Num>` (прочерк отличим от нуля, запятая,
      табличные цифры, округление половины вверх) и `<DataTable>` (одна
      реализация на обе ширины: шире 1080 px таблица, уже — список с
      раскрытием по тапу, доступный с клавиатуры)
- [x] **Клиент API и сессия:** 401 → одно продление с тремя исходами, 403
      наверх без продления, `/auth/*` мимо продления. Четыре исхода проверки
      сессии; 401 посреди работы сбрасывает сессию; выход очищает кэш и
      работает при недоступном сервере
- [x] **Оболочка:** шапка с именем и ролями, меню на восемь разделов,
      заглушки с описанием. Прогон на голове `frontend-faza-2`: 60 тестов
      фронтенда, 177 бэкенда
- [x] **Раздача и CI.** `infra/nginx/Dockerfile` собирает фронтенд и кладёт
      в образ nginx; ассеты `immutable` на год, `index.html` — `no-cache`.
      В CI живая проверка образа: сборка, `nginx -t` и восемь curl-проверок
      заголовков и маршрутов
- [x] **Экраны.** Справочник, блюда, карточка блюда, сверка справочника
      (три группы врозь, различие подсвечено в имени кандидата,
      автоподстановки нет, кнопки действий недоступны до фазы записи).
      Сбой фонового обновления не стирает показанные данные. Прогон: 111 тестов
      фронтенда, 179 бэкенда; `npm run build` собирается
- [x] **Домен и TLS.** `art.karppacho.ru` → 153.80.247.91, сертификат
      Let's Encrypt до 21.12.2026, продление — крон root по понедельникам,
      пробный `renew --dry-run` прошёл
- [x] **Выложено.** `deploy.sh` за 23.09 прошёл целиком пять раз; версия, на
      которой сделаны проверки ниже, — 3e8ee02d: «Готово», `/healthz` на
      сервере отдаёт этот хеш, `env` production.
      Снаружи: `/` 200 с HSTS и CSP, `/api/me` без куки 401, http → 301,
      `/healthz` 404; отвечают только 22, 80 и 443
- [x] **Проверено вживую.** Вход через сайт, куки `Secure` и `HttpOnly`;
      130 ингредиентов, 130 блюд, B001 — 84,66 ₽, сверка 101/85/16. На
      ширине 360 px страница не прокручивается вбок ни на одном экране и ни в
      одной из 130 карточек (Playwright). С подменой ответов в браузере:
      сбой справочника — блок 98 px с «Повторить» в окне (был на всю высоту);
      подпись «проверьте единицы измерения» видна на телефоне в карточке и
      в списке
- [x] **Ночной дамп базы** — крон в 04:00, хранится 14 дней; прогон вручную
      дал 140 КБ

**Осталось**

- [x] **Слито.** PR №11 (`frontend-faza-2`) и №12 (`frontend-faza-2-ekrany`)
      — в `main` 23.09.2026 merge-коммитами, CI на `main` зелёный
- [x] **Сервер на `main`.** 23.09.2026 `deploy.sh` прошёл целиком:
      «Готово. Версия b739210e»; `/healthz` на сервере отдаёт
      b739210ea3dd…, `env` production; снаружи `/api/me` без куки — 401
- [ ] Обязательные проверки «фронтенд» и «образ nginx» в защите `main` —
      действие Александра в настройках GitHub
- Расчётка и конкуренты в интерфейсе переезжают: расчётка — в фазу 3,
  конкуренты — в фазу 4 (их сперва надо импортировать: листы читаются, но
  в базу не переносятся)

*Выход: шефу есть чем пользоваться, сломать нечего.*

### Что нашло ревью перед слиянием

Выкладка была сломана в пяти местах, и все пять чинились до слияния.
Шестое и седьмое нашлись только на первой настоящей выкладке — в таблице
последними:

| Что | Чем грозило |
|---|---|
| Сертификат писался в хостовый `/etc/letsencrypt`, а nginx читал именованный том | nginx не поднялся бы, а без него нет и пути `acme-challenge` для выпуска — замкнутый круг |
| `deploy.sh` снимал дамп через `exec -T postgres`, а такого сервиса в compose нет | выкладка вставала на втором шаге всегда |
| Смоук деплоя ходил на `127.0.0.1:8080` мимо nginx | при лежащем сайте выкладка отчиталась бы «Готово» |
| `.env.example` с `SESSION_COOKIE_SECURE=false` | боевой `.env` создают из него — куки с токеном ушли бы без `Secure` |
| 401 и 403 от шлюза Supabase сводились к «Неверная почта или пароль» | забытый anon-ключ — и шеф перебирает пароли, упираясь в лимит |
| `deploy.sh` и `setup_tls.sh` лежали в git без бита исполнения (коммиты с Windows) | `./scripts/…` на сервере — «Permission denied». Теперь это ловит тест |
| Пустой `kitchen.worker` без `__main__.py` стоял в compose без профиля | `up -d` поднял бы контейнер, падающий по кругу; смоук этого не видит |

**Проверки перед первой выкладкой — сделаны 23.09.2026:** сертификат
выпущен настоящий; стек Supabase поднят, сеть `supabase_default` есть;
`pg_dump -U postgres` внутри `supabase-db` проходит (592 КБ, код 0); в
`.env` адрес Supabase — `http://api-gw:8000`. `setup_tls.sh` по-прежнему
нельзя прерывать между удалением заглушки и certbot (`trap` нет) — поэтому
на сервере он запускался через `nohup setsid`.

**Замер сверки на живых данных (10.09.2026):** 101 карточка в базе,
85 склеено, **16 требуют решения** — 3 тёзки, 4 кандидата, 9 без пары.

Почему 101, а не 108, как строк в листе: одна строка (69) вообще без
имени, и **шесть пар дублируются по имени**. Ключа кроме имени у карточек
нет, поэтому вторая затирает первую. Дубли:
«Булочка для датского хот дога», «Креветки в панировке», «Рыбные палочки»,
«Соус барбекю РБК», «Соус брусничный», «Тостовый хлеб».

Три из шести — те же названия, что дублируются и в справочнике `ING`.
Похоже, одни и те же продукты заведены дважды в обеих таблицах; шефу
стоит посмотреть, что из этого лишнее.

---

## Фаза 3 — запись

Порядок решён 23.09.2026 (см. «Где мы сейчас»): сперва путь записи,
первая пишущая функция — карточки по этикетке, расчётка — после них.

- [ ] Предупреждение о штучных позициях: нетто меньше 10 % веса штуки или
      0 г — замечание «похоже, вписано количество штук». Себестоимость не
      меняется; эталон перепривязывается только по замечаниям (с
      объяснением). Порог проверен на живых данных: у 21 строки доля ≤ 4 %,
      у остальных ≥ 30 %
- [ ] Фильтры и сортировка как в Google-таблицах: сортировка по любой
      колонке, фильтр значений с галочками (категория, единица, статус,
      карточка; у блюд — замечания, «выше цены»), состояние в адресе, свой
      вид на телефоне
- [ ] ADR-0003 «платформа пишет в таблицу»: очередь записи, сверка ячейки
      с последним прочитанным значением перед записью (изменилась —
      конфликт), журнал правок, повторное чтение; снапшоты листов в БД
      вместо файлов. Вместе с ним — снятие `BOTS_ALIVE` (фаза 5)
- [ ] **Карточки ингредиентов по этикетке** — замена `pizza_bot_new`, которой
      у шефа нет с 08.09: снять этикетку телефоном, распознать состав и сроки
      (polza.ai, Vision), поправить, три фото продукта, согласование. В ТЗ
      есть (раздел 1.1), в меню стоит заглушкой. Упирается в правило
      владения: лист карточек пишет бот, и пока `BOTS_ALIVE = True`,
      `INGREDIENT_CARDS.writable()` пуст. Решено 23.09: боты не вернутся,
      флаг снимается вместе с путём записи; карточки — первая пишущая
      функция, раньше расчётки
- [ ] Редактирование справочника и блюд прямо в таблицах экранов — на
      пути записи, с конфликтами и журналом правок. Человеческие колонки
      (цена меню, декларация, халяль) автоматика не пишет; правка их
      человеком через интерфейс — вопрос спеки
- [ ] Расчётка: из 10 колонок нам принадлежат 9
- [ ] LLM-чат с потоковым выводом и настоящими таблицами
- [ ] Учёт расходов на модель: `cost_rub` polza.ai возвращает, сейчас теряется

**Поправка к плану.** Карточки ингредиентов я считал низкорисковыми, потому
что их не трогает `kitchen_bot`. Но их пишет `pizza_bot_new`, и правило
владения это поймало: `INGREDIENT_CARDS.writable()` пуст. Начинать записью
с них нельзя, пока флаг поднят. С 23.09 это снимается: боты не вернутся
(решение Александра), и флаг опускается вместе с путём записи.

---

## Фаза 4 — дегустации и конкуренты

- [ ] Дегустации целиком, жюри по ссылке-токену без регистрации
- [ ] Перенос SQLite конкурентов: 6775 позиций, 675 изменений с июля
- [ ] Скрапинг в воркере; вернуть `chrome-debug`, `xvfb` (сейчас выключены)

Данные для этой фазы лежат в `/home/artem/archive/bots-stopped/kitchen_bot/data/`:
`competitors.db` и профили браузера с вручную пройденными капчами.
Воссоздать их невозможно.

---

## Фаза 5 — вывод ботов

Боты уже не работают, но формально не выведены: `BOTS_ALIVE = True` в
`kitchen/sync/ownership.py`.

- [ ] Снять флаг — это открывает запись в общий справочник. Решено
      Александром 23.09.2026: боты не вернутся; делается вместе с путём
      записи фазы 3
- [ ] Убедиться, что все три бота действительно остановлены: юниты сняты и
      `disable` с 09.09 — перед снятием флага проверить `systemctl is-enabled`

Снятие флага **не** открывает человеческие колонки: цены коммерсов и цену
меню. Закреплено тестом `test_retiring_bots_unblocks_shared_but_not_human`.

---

## Долги и открытые решения

| Что | Почему отложено |
|---|---|
| Ротация секретов | Решение Александра, осознанный риск |
| Магик-ссылки для входа | Нет SMTP-релея; письма с РФ-VPS уходят в спам |
| Фото на Drive публичны всему интернету | Чинить при переносе фото в фазе 3 |
| `mir-event-demo-bot` | Демо для другого заказчика, заархивирован |
| Тепловые потери читаются, но не применяются | Осознанный пробел, унаследован от бота |
| Строки 132–169 в `ING` без id (38 штук) | Что это за блок — вопрос к шефу |
| Шесть пар карточек дублируются по имени | Импорт предупреждает; чинится правкой листа |
| Строка ТТК 697 (B129) без ссылок | Признанное расхождение с эталоном |
| Мелочи ревью первой части фазы 2 (ни одна не ломает показ) | После слияния, по порядку риска: подделка выхода с чужого сайта; флаги `delete_cookie`; потеря повёрнутых токенов при падении `_whoami`; ограничение частоты nginx отвечает 503, а не 429, и вход показывает «Не удалось получить данные»; 422 от `EmailStr` без своего сообщения; кнопка «Выйти» на экране отказа; `front-build` не входит в `make check`; предупреждения React Router о флагах v7 в тестах; сужение CORS |
| Пробелы в тестах экранов | Клавиатурный переход по строке на широком экране; `.vizualno-skryto` объявлен дважды; заголовок колонки «id» не говорит, что это id из таблицы; на сверке нет теста «справочник ещё грузится»; нет теста `NameDiff` на совсем разные имена |
| Узкий вид состава в карточке блюда | На 360 px таблица состава шире экрана и прокручивается внутри своей рамки: «Стоимость» и «Доля» видны только после сдвига. ТЗ (раздел 2) хочет на узком экране список с главными числами. План задачи 12 дал обычную таблицу — пробел плана; очерёдность — решение Александра |
| Сортировка блюд по марже | Требование ТЗ (раздел 5.3) потерялось между ТЗ, спекой и планом, а это главный вопрос экрана «у каких блюд просела маржа». `sravnit` не понимает минус (маржа бывает −399,3) — сортировать на сервере или научить знаку. Войдёт в фильтры и сортировку (порядок работ, п. 3) |
| `NameDiff` не показывает вставку | «Огурцы резаные» против «Огурцы не резаные» — пометки нет: подсвечивается только различие в имени кандидата. Сегодня не бьёт (у тёзок имена равны, кандидатов «похожего» бэкенд не отдаёт); закрыть до ручки подбора, с тестами на вставку и удаление в обе стороны |
| Штучные позиции с весом 1–2 г в ТТК | 26 блюд: похоже, в ТТК вписано количество штук, а расчёт ждёт граммы; себестоимость занижена (B001: круассан 0,83 ₽ вместо 49,50 ₽). Эталон старого бота даёт то же — ошибка унаследована. Решено 23.09: предупреждение в расчёте (порядок работ, п. 1) и правка листа шефом |
| Бэкап базы не проверен восстановлением | Ночной дамп идёт, но ни разу не разворачивался в пустую базу; копии лежат на той же ВМ, что простояла 18–22.09, — нужна копия вне сервера |
| Мелочи итогового ревью второй части | Выбранный статус пропадает из списка вариантов, пока идёт новый поиск (`keepPreviousData`); классы без стилей `.kartochka*`; `Number()` над долей КБЖУ (предписано планом); одна цена в двух форматах — «18,5» в справочнике и «18,50» на сверке; `aria-label` на `<span>` без роли; «← Блюда» теряет фильтры; строка таблицы — `tr` с `tabIndex`, а не ссылка; `Pokazatel` написан дважды; плотность — 24 строки на 1920×1080 при требовании 25–30 |
| Отложенное из журнала работ | `Number()` проходит мимо запрета eslint; полные наборы шрифтов в сборке; в CI `if: failure()` не срабатывает при таймауте задачи; незакрытый `httpx.Client` приложения; нет тестов «refresh → 502»; нет `aria-invalid` на форме входа; шкала кеглей; `DATABASE_URL` в `.env.example` указывает на 127.0.0.1, а из контейнера нужен `supavisor`; смоук `deploy.sh` ходит в nginx с `-k` и после выпуска настоящего сертификата — просроченный или чужой сертификат выкладку не остановит |

## Как вести этот файл

Пункт переезжает в «сделано» только вместе со строкой доказательства.
Найденное по дороге — в раздел фазы, а разбор инцидента — в журнал в
[CLAUDE.md](../CLAUDE.md). Роадмап отвечает на вопрос «что дальше»,
журнал — на вопрос «почему так сделано».
